# OBS000013 - コスト構造改善: ATRトレール幅拡大によるK5m/K4m突破（方向性a）

> 担当: 未割当（着手時に S 戦略チームが割当・SYS-FX番号を採番）
> 起票: 2026-08-28
> 発端: EXP-FX000020でSYS-FX011/012/018/025全4構成がK5m（スプレッドコスト倍率≥3.0）
> 未達でREJECT確定した後、司令塔が「本PJの方向転換」として (a) コスト構造の改善 を選択
> 診断: `scripts/diagnose_trail_multiplier_headroom.py`
> 出力: `research/method-notes/trail_multiplier_headroom_diagnostic.json`
> 位置づけ: **正式プロトコル外の探索的診断**（OBS000011の
> `explore_grid_strategy_sanity_check.py`と同じ位置づけ）。まだSYS番号・EXP番号は
> 採番していない、着手前の一次診断。Train期間（現行GMOデータ、2023-11-01〜2025-03-31）
> のみ使用、簡易R指標（equity曲線を使わない$非依存の簡略Sharpe等）で判定しており、
> **正式パイプライン（$複利サイジング・ピーク比DD・permutation test）では未確認**

## 1. 仮説（結果を見る前に記録）

K5m = mean(r_gross) / mean(cost_r + commission_r) は、`initial_risk`（=1R、
stop_buffer_atr_m5 × ATR(M5)）のスケーリングだけでは動かせない（r_gross・cost_rの
両方が同じ 1/initial_risk で割られ、比率としては打ち消し合うため）。実質的に
K5m ≈ mean(価格変動幅) / mean(固定pipsコスト) であり、これを改善する筋の良いレバーは
「勝ちトレードが実際にどれだけ長くトレンドを追えるか」＝ATRトレール幅の設計である。

SYS-FX011の現行トレール幅（atr_trail_multiplier_m5 = stop_buffer_atr_m5 × 1.0）は
損切り幅と同じ1R相当と非常にタイトで、拡張Trainのペイオフレシオが1.07（ほぼ1:1）と
「トレンドフォロー型の出口設計」にしては低かった。これはトレールが早く効きすぎて、
勝ちトレードでもトレンドを十分に追えていない可能性を示唆していた。

## 2. 診断結果: Train単独・4通貨・R指標ベース（2026-08-28）

トレール幅を stop_buffer_atr_m5 の {1.0, 1.5, 2.0, 3.0} 倍でスイープ。エントリー・
損切り幅（=1R定義）・スプレッド/スリッページ/手数料モデルは一切変更せず、
`simulate_dow_theory_trend()`の`trail_mult`引数のみを変えた（正式パイプラインの
ファイルは変更していない、診断スクリプト内で一時的に上書き）。

| trail倍率 | n | 勝率 | K5m | ペイオフ | PF(R) | 平均r_net | Sharpe(R,簡易) |
|---|---|---|---|---|---|---|---|
| 1.0x（現行） | 524 | 60.3% | 2.28 ❌ | 1.16 ❌ | 1.76 | 0.329 | 0.914 |
| 1.5x | 482 | 56.9% | 2.62 ❌ | 1.46 ❌ | 1.93 | 0.404 | 0.881 |
| 2.0x | 431 | 55.0% | 2.90 ❌（僅差） | 1.71 ✅ | 2.09 | 0.471 | 0.876 |
| **3.0x** | **355** | **51.8%** | **3.95 ✅** | **2.53 ✅** | **2.72** | **0.699** | 0.732 |

**3.0倍で K5m（≥3.0）・ペイオフ（≥1.5）とも明確にクリアし、実効トレード数も355と
min_n_trades=300を上回ったまま。** 単調な改善傾向で、トレールを広げるほど勝率は
下がるが1トレードあたりの平均値幅が伸び、コスト比・ペイオフともに改善している。

## 3. 重要な留保（正直な記録、過度な期待は禁物）

- **R指標ベースの簡易診断であり、正式パイプライン（$複利サイジング・ピーク比DD・
  permutation test・K3m・K6m等）では一切確認していない**。SYS-FX023で「R平均ベースの
  簡易指標は正式パイプラインで再現しないことがある」という教訓が繰り返し当てはまる
  リスクがある（EXP-FX000018でも探索診断がまさにこの理由で符号反転した）
- **Sharpe(R,簡易)は倍率を上げるほど悪化傾向**（0.914→0.732）。これは月次分散が
  拡大しているためで、K5m/K4mが改善してもリスク調整後リターンが改善するとは限らない
- **トレード数が524→355へ32%減少**。トレール幅拡大で早期反転による再エントリー
  機会が減るため。min_n_trades(300)には残るが、Validationでは実効nがさらに下がる懸念
- **Train単独のみ**。Validationでの再現性は完全に未確認（SYS-FX009/010型の過学習が
  この project で繰り返し起きているため、無条件に信頼すべきではない）
- **拡張Train（Dukascopy、EXP-FX000020）ではなく現行GMO期間のみ**で診断した
- 手数料・スプレッド・スリッページのコストモデルは変更していない（変えたのは
  トレール幅のみ）

## 4. 次のアクション（提案）

正式プロトコル（HARKing防止: 結果を見る前に評価基準を固定）に従い、以下を提案する:

1. **SYS-FX026として正式起票**（SYS-FX011の出口設計を再設計した派生、
   SYS-FX018がSYS-FX012の派生として採番された前例に倣う）
2. **候補は3.0倍のみを事前登録**（本診断のスイープはあくまで一次探索であり、
   正式パイプラインでの選定は3.0倍1点に絞る。2.0x等の中間値を正式パイプラインで
   併せて試すと「良い方を選ぶ」HARKingになるため行わない）
3. 正式$パイプライン（`backtest_vol_breakout_dow_theory_4pairs_v7_trailonly_1000usd.py`
   相当、複利1%リスクサイジング）でTrain評価 → K1m〜K7m・実効n・permutation_pの
   正式判定
4. Train通過した場合のみValidationを参照（過学習検出プロトコルに従いパラメータ
   再導出なし）

## 追記

- 2026-08-28: 起票。司令塔が選択した方向性(a コスト構造の改善)の一次診断として実施。
  正式パイプラインでの検証はEXP-FX000021（SYS-FX026）として進める
