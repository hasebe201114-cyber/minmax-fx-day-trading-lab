# OBS000007 - SYS-FX007 バックテスト実装正当性に対する Adversarial レビュー

> 担当: C 品質チーム（adversarial-reviewer 視点。独立した Opus エージェントによる再監査 + 一部を本セッションで直接コード確認）
> 起票: 2026-08-14
> **更新: 2026-08-15（差し戻し 1〜5・7(一部) の修正実施。修正過程で新規バグ [PULLBACK_CONFIRMED 状態固着] も発見・修正。判定は引き続き保留 — 詳細は末尾の追記参照）**
> **再更新: 2026-08-15（1時間毎の自動監視ルーティンが、直前の自己修正コミットを独立 Opus エージェントで再監査。5件の主要バグ修正は実データで再現・裏付け確認された一方、差し戻し7の本来の合格基準（保有期間が中期スイング）は未達成であること、データ破損の根本原因が未修正のまま残っていたことが判明。後者は本追記内で修正済み。詳細は末尾の追記2を参照）**
> 対応 OBS / 検証: OBS000003（全結果）、OBS000004（AUD/JPY 判定）、OBS000005（プロセス構造）、OBS000006（パラメータ根拠）
> 対応 SYS: SYS-FX007（レンジブレイク・プルバック戦略 v2.1）
> 方針: 良好結果はもちろん、**不採用という結論も含めて疑う**。OBS000004/005/006 はいずれも「バックテストが spec の戦略を正しく測っている」ことを暗黙の前提にしていたが、本書はその前提自体を検証する。

## 最終判定

- [ ] 採用可
- [ ] 不採用
- [x] **保留（ブロッキング）— 現行バックテスト実装による全ての結果を判定材料として使用停止**

これまでの一連のレビュー（OBS000004: AUD/JPY 不採用確定、OBS000005: プロセス是正が機能したと評価）は、**実装が実際には SYS-FX007 とは異なる、より単純で先読みバグを含む戦略を測っていた**ことに気づいていなかった。したがって「不採用」という結論そのものが、正しい実装での再検証まで確定できない。

## 背景

司令塔の指示により、モデル切り替え前の手戻り防止として、独立した Opus エージェントに OBS000003〜006 の内容をコード・データに照らして再監査させた。その結果、4 文書のいずれも指摘していなかった、バックテストエンジン自体の実装不備が複数発見された。うち最重要の 4 件（本書 §チェック 1〜4）は、本セッションで実際のソースコード・データを読んで独立に再現・確認した。

## チェックリスト（各項目に証拠）

| # | チェック項目 | 結果 | 証拠 |
|---|---|---|---|
| 1 | SL/TP 判定が未確定の未来データを参照していないか | ❌（確認済み・最重大） | `src/minmax_fx_dt/backtest/runner.py` の ST バーループは、各 ST（M15）タイムスタンプに対応する H4 バーを `mt_ohlcv.index[mt_ohlcv.index <= st_ts][-1]` で特定した後、`mt_ohlcv.loc[current_h4_ts]` から**そのバーの確定済み高値・安値**（`h4_row["high"]`/`h4_row["low"]`）を取得し、SL/TP 判定に使っている。H4 バーは 4 時間分の区間だが、この高安は**バー開始直後の ST タイムスタンプの時点でも、バー終了時点の値がそのまま使われる**。つまり最大 4 時間先の高安を既知として SL/TP を判定している。ST バー自身の高安（`st_high`/`st_low`、`runner.py` で計算されるが以後未使用のデッド変数）は使われていない。 |
| 2 | エントリー価格が未確定の未来データを参照していないか | ❌（確認済み） | エントリー価格は M15 バーの終値（`multi_timeframe.py` の `st_close.iloc[-1]`）を使うが、これは当該 15 分バーが**確定した後**の値であり、ST ループでその時点（バー開始時点）の意思決定として扱われている。加えて MT（H4 終値でのブレイク判定）・LT（進行中 D1 の SMA/ADX）でも同様の「進行中バーの確定値」を使う構造になっており、最大 4 時間（MT）・24 時間（LT）の先読みが入りうる。 |
| 3 | エントリーロジックのステートマシンがバーをまたいで永続化されているか | ❌（確認済み） | `multi_timeframe.py` の `evaluate_mtf()` は呼び出しのたびに `RangeBreakoutEngine` を**新規生成**し（`engine = RangeBreakoutEngine(...)`）、`process_h4_bar` を 1 回だけ呼んで終わる。`runner.py` は ST バーごとにこの `evaluate_mtf()` を呼んでいるため、**エンジンの状態は 1 バーごとにリセットされる**。結果、ドキュメント化されたステートマシン（レンジ形成中 → ブレイク検出 → 戻り待ち → 戻り確認 → エントリー、という時間的に分離した遷移）は実質的に機能せず、「その場のバーで Donchian 境界外に終値があり、LT 方向が一致していれば即エントリー」という単純化されたロジックが動いている。 |
| 4 | エントリー条件 5 つ（4 条件 AND、MT-3 除く）が実質的に独立した制約として機能しているか | △（実質 2 条件） | 条件 3（S/R ライン）は許容誤差が価格の 0.5%（USD/JPY で約 75 pips）と極めて広く、ほぼ常にどこかの検出済み S/R が範囲内に入る。条件 5（ST 戻り確認）はブレイク検出と同一バーで評価されるため、時間的分離のある「戻り待ち」を経ずにほぼ自動成立する。実質的に効いているのは「LT 方向一致」と「MT-1 Donchian ブレイク」の 2 条件のみと考えられる。 |
| 5 | EUR/USD 全敗の真因は「pips スケール問題」（OBS000003/006 の説明）か | ❌（別の実装バグ） | `scripts/ablation_sweep.py:188` に `is_jpy_pair=True` が**全ペア共通でハードコード**されている（本セッションで確認済み）。`--pair EUR_USD` で実行してもこの値は変わらないため、EUR/USD の pip 換算が 100 倍狂う（0.0001 が正しいところ 0.01 として計算される）。この結果、スプレッド・スリッページが実質 30〜50 pips 相当に膨張し、コード上は BUY の約定価格が TP 価格を超えて設定される事態が発生しうる。一方 `scripts/run_train_val_test.py:234` は `is_jpy_pair="JPY" in symbol` と正しく実装されている。 |
| 6 | ATR ベース SL が実際に通貨間でスケール依存しているか（OBS000006 の主張） | ❌（実データで否定） | `data/curated/ds-1.json` から H4 ATR(14) 中央値と M5/M15 レンジ分布を算出したところ、EUR/USD の SL（1.5×ATR ≈ 31 pips）は M15 ノイズの 95 パーセンタイル（12.2 pips）の約 2.6 倍であり、他 4 通貨（2.44〜2.50 倍）とほぼ同一水準だった。ATR ベース決済は定義上スケール不変であり、EUR/USD が外れ値ではない。 |
| 7 | OBS000004 の TVT「不採用確定」と OBS000003 §10 の 2024 単年結果が同一プリセットの比較か | ❌（交絡あり） | `ablation_sweep.py` の A1_A2_combined は `reward_risk_ratio=2.0`（本セッションで確認）に対し、`run_train_val_test.py` の同名プリセットは `reward_risk_ratio=3.0`（同確認済み）。TP 距離が 1.5 倍異なる。加えてスプレッド固定値・スワップ適用有無・`is_jpy_pair` 判定方法も異なり、単純な「2024 単年 vs Train/Val/Test」比較にはなっていない。 |
| 8 | TVT（train/val/test）の入力データがリポジトリから再現可能か | ❌（確認済み・ブロッキング） | `data/curated/ds-1.json` の実際のカバレッジは全 5 通貨とも **2024-01-02T07:00 〜 2025-01-01T05:55（74,232 本）のみ**（本セッションで直接確認）。しかし TVT は Train (2020-2022)・Validation (2023-2024H1)・Test (2024H2-2025) の 3 期間、244〜321 件/セルのトレードを報告している。`run_train_val_test.py` が参照する `data/curated/mtf_cache/` はリポジトリに存在せず（`data/curated/` の実際の内容は `ds-1.json` と `ds-7.json` のみ、git 管理下にも `mtf_cache` は 0 件）、`data/MANIFEST.json` も 2024 年 1 ペア分の記録のみ。**TVT 15 セルの結果は、現在のリポジトリの状態からは生成し得ない**。 |

## 反論

### 論点 A: OBS000003 §1「シグナル品質はクリーン」「130 件すべてが 5 条件 AND 成立」は事実として空虚に真
チェック #4 の通り、5条件のうち3条件（S/R許容誤差75pips、戻り確認の同時判定、MT-3恒久フォールバック）はほぼ自動成立する。「全件が条件成立」という記述は技術的に正しいが、それが意味する「厳格なフィルターを通過した高品質シグナル」という含意は誤りで、実質「LT方向 + Donchianブレイク」の単純な2条件ロジックが動いていた。

### 論点 B: OBS000003 §3.4「SL が83%」「損大利小」の解釈は先読みバグの影響下にある
チェック #1 の通り、SL/TP判定に進行中H4バーの確定高安（最大4時間先の情報）を使っている。SLがTPより先に判定される実装（`simulator.py`のcheck_stop_loss_take_profit）と組み合わさると、H4バーのレンジがSL・TP両水準を跨ぐケースは機械的にSL判定になる。§4の失敗仮説1〜5（プルバック早すぎ・SL近すぎ・TP近すぎ等）は、いずれもこのバイアスの影響を切り分けずに立てられている。

### 論点 C: OBS000004の「2024単年の好成績は統計的偶然」という結論は、交絡した比較から導かれている
チェック #7 の通り、比較対象のプリセットがRR・スプレッド・スワップ・pip換算方式で異なる。AUD/JPYの2024単年結果とTVT結果の差は、「out-of-sampleで化けが剥がれた」以外に「そもそも別のパラメータセットで走らせた」ことでも説明がつく。不採用の結論自体は他の要因（チェック#1-4のバグ）でも支持されうるが、OBS000004が使った論拠（「同一プリセットでの比較」）はそのままでは成立しない。

### 論点 D: OBS000005の「プロセス是正が機能した」評価は、証拠保全の観点で時期尚早
チェック #8の通り、TVTを生成した入力データがリポジトリから再現できない。プロセスとして「train/val/testを実行した」こと自体は前進だが、**その成果物が監査可能な形で保存されていない**ため、「是正が機能した」という評価は「是正を試みた」レベルに留める必要がある。

## 差し戻し（該当あり、最優先）

- 差し戻し先: **B 実装チーム（quant-researcher）**
- 内容（他のいかなる再走行よりも先に完了させる）:
  1. **`runner.py` の SL/TP チェックを ST バー（M15）自身の高安を使うよう修正**。H4 バーの確定高安を使うのは、そのH4バーが完全に過去のものになってから（次のH4バー開始後）に限定する
  2. **MT/LT の評価に使う H4/D1 データを「確定済みバー」のみに制限**（進行中バーの終値を使わない）。エントリー価格の評価も同様
  3. **`RangeBreakoutEngine` をバーをまたいで永続化**し、ドキュメント化されたステートマシン（レンジ形成→ブレイク検出→戻り待ち→戻り確認→エントリー）を実際に時間的に分離した形で動かす
  4. **条件3（S/Rライン）の許容誤差を再検討**（現状価格の0.5%は広すぎる）
  5. **`ablation_sweep.py:188` の `is_jpy_pair=True` ハードコードを `run_train_val_test.py` と同様の動的判定に修正**
  6. **TVT用データ（2020-2025 OHLCV）を正式に取得し、`data/MANIFEST.json`・`data/CATALOG.md` に記録**。取得できるまで、TVT結果は「未検証」として扱う
  7. 1〜6完了後、Base 1本のみで再走行し、SL/TP内訳・保有期間分布が仕様の中期スイング（数日〜数週間）に近づくか確認してから、他の探索を再開する

## 結論

OBS000004（AUD/JPY「不採用確定」）・OBS000005（「プロセス是正が機能した」との評価）は、上記の実装不備が判明する前の暫定的な結論として扱い、**修正・再検証まで最終確定を保留する**。OBS000006（パラメータ根拠、判定: 保留）はむしろ本書によって論拠が強化される — パラメータ以前に、実装が仕様の戦略を体現していなかったことが判明したため。

## 追記（2026-08-15）: 差し戻し 1〜5・7 の修正実施結果

司令塔の「引き継ぎして進めてください」との指示を受け、差し戻し 1〜5 および 7（一部）を本セッションで実施した。差し戻し 6（TVT用データ再取得）は着手したが未完了(下記参照)。

### 実施内容

| # | 差し戻し | 対応 |
|---|---|---|
| 1 | SL/TP の先読み修正 | `src/minmax_fx_dt/backtest/runner.py`: SL/TP チェックを進行中 H4 バーの確定高安ではなく、**ST(M15) バー自身の高安**を使うよう変更 |
| 2 | MT/LT の先読み修正 | 同ファイル: LT(D1)/MT(H4) の評価に使うスライスから、`st_ts` が含まれる未確定バーを除外し、**1 本前までの確定済みバーのみ**を渡すよう変更 |
| 3 | ステートマシンの永続化 | `src/minmax_fx_dt/strategy/multi_timeframe.py`: `evaluate_mtf()` に `engine`/`process_h4` 引数を追加し、`runner.py` 側で `RangeBreakoutEngine` をバーをまたいで保持。`process_h4_bar()` は新たに 1 本確定した H4 バーがある場合のみ呼ぶよう変更 |
| 4 | S/R 許容誤差の見直し | `src/minmax_fx_dt/strategy/range_breakout.py`: 条件 3 の許容誤差を `price × 0.5%`(≈75 pips) から `ATR × 0.5`(≈24 pips, USD/JPY) に変更 |
| 5 | `is_jpy_pair` ハードコード修正 | `scripts/ablation_sweep.py`・`scripts/run_backtest_real.py`: `is_jpy_pair=True` 固定を `"JPY" in symbol` に修正。あわせてスプレッドも通貨ペア別 (`SPREAD_PIPS`) に統一 |
| 7 (一部) | Base プリセット再走行 | 下記参照 |

既存テストスイート (`tests/test_strategy.py`, 11 件) は全て pass を維持。

### 新規発見: PULLBACK_CONFIRMED 状態からの脱出遷移が存在しなかった

差し戻し 3（ステートマシン永続化）を実施した直後、USD/JPY・AUD/JPY のいずれも **2024 年通して 1 トレードしか発生しない**という結果になった。原因を調査した結果、`RangeBreakoutEngine.process_h4_bar()` には `PULLBACK_CONFIRMED` 状態を扱う分岐が存在せず、一度エントリーが成立すると**エンジンが永久に `PULLBACK_CONFIRMED` に固着し、以後二度とエントリーできなくなる**という、独立監査でも指摘されていなかった潜在バグを発見した。旧実装はエンジンを毎回使い捨てていた（差し戻し 3 の対象そのもの）ため、この固着バグは表面化していなかった。

`range_breakout.py` に `PULLBACK_CONFIRMED` → (レンジ復帰なら) `RANGE_FORMING` / (レンジ外継続なら) `TREND_UP`/`TREND_DOWN` の遷移を追加し、修正した。

### Base プリセット再走行結果 (USD/JPY 2024, 修正前後比較)

| 段階 | n_trades | 勝率 | PF | 月次シャープ | 備考 |
|---|---|---|---|---|---|
| 修正前 (OBS000003 §3 の元結果) | 130 | 15.4% | 0.245 | -3.876 | 先読み + 状態固着なしバグの影響下 |
| 修正1〜4適用直後 (PULLBACK_CONFIRMED 固着バグ未修正) | **1** | 100% | inf | 1.000 | エンジンが 1 トレード後に永久固着 (新規発見バグ) |
| PULLBACK_CONFIRMED 修正後 | **17** | 11.8% | 0.174 | -2.663 | 先読み・固着とも解消した状態 |

AUD/JPY でも同様に 1 トレード → (PULLBACK_CONFIRMED 修正後は未再実行、USD/JPY の傾向から同様の増加が見込まれる) だった。

**解釈**: 旧実装 (130 件) は先読みバグにより過大に取引していた。修正後 (17 件) はより現実的な頻度だが、それでも **依然として PF 0.174・シャープ -2.663 と負け**、しかも n=17 は spec の有効サンプル基準 (60 件) に遠く及ばない。この結果だけでは「戦略コンセプトにエッジがない」とも「実装がまだ不完全」とも断定できない。**修正は完了したが、これは新たな1回の試行であり、5通貨×複数年のTVT再実行と permutation test を経なければ結論は出せない**(OBS000005の「改善ループに停止条件を」という指摘を踏まえ、ここでこれ以上パラメータをいじって"良い数字"を探すことはしない)。

### 差し戻し 6: TVT用 2020-2025 データ再取得の状況

`scripts/data/fetch_ds1_ohlcv.py`(GMO 公式 Public API、APIキー不要)で 2020年・2022年・2023年の複数日でテスト取得を試みたところ、**日付に関わらず全て `ERR-5201: MAINTENANCE` エラー**となった。GMO の公式ステータスエンドポイント (`https://forex-api.coin.z.com/public/v1/status`) を確認したところ、確認時点 (2026-08-15 土曜 00:25 UTC) で `"status": "MAINTENANCE"` であることを確認 — **土曜のGMO定期メンテナンス期間中であり、データが取得できないこと自体はデータ欠如ではなく一時的なもの**と判明した。

取得スクリプト自体の軽微なバグ (`results` が空のときに `KeyError: 'file'`) も発見し修正した。

**テスト中の事故と復旧**: テスト取得の過程で `data/curated/ds-1.json` の USD_JPY データが一時的に破損 (74,232本→852本に縮小) したが、git 管理下だったため即座に `git checkout` で復元した。**教訓として、次回の本番取得は `ds-1.json` を直接上書きせず、別ファイルに取得して検証後にマージする運用に変更すべき**(本追記の差し戻しに追加)。

**残作業 (差し戻し 6 未完了分)**:
- GMO メンテナンス時間外 (平日) に本番の 2020-2025 全 5 通貨取得を再実行する (推定約 9,000 リクエスト、レート制限込みで数十分〜1時間規模)
- 取得は一時ファイルに行い、検証 (件数・期間・欠損チェック) 後に `data/curated/ds-1.json` を置き換える
- `data/MANIFEST.json` / `data/CATALOG.md` を更新する
- 取得完了後、TVT (train/val/test) を修正済みエンジンで再実行する

## 追記2（2026-08-15）: 1時間毎自動監視ルーティンによる独立 Opus 再監査

前追記（差し戻し1〜5・7一部の修正、commit `4490a6e`）を、司令塔が設定した「リポジトリ全体を1時間ごとに監視し、新規コミットを独立Opusエージェントでレビューする」ルーティンが検知し、独立して再監査した。監査手法: パッチの読解に加え、変更後の全ファイルを実際に読み、タイムスタンプスライシング・ステートマシン遷移を追跡する検証プログラムを自作して実行、さらに実際に USD/JPY 2024 のフルバックテストを再現実行して数値を突合した。

### 確認された正しい点（抜粋）

- SL/TP 先読み修正・確定バーのみでのスライス・エンジン永続化・`process_h4`ゲート・`is_jpy_pair`修正は、いずれも**具体例を使った手計算・実データ検証で正しいことが確認された**（旧トラッキング変数`last_h4_*`は grep で完全に削除確認済み）
- PULLBACK_CONFIRMED 修正: **修正を外した反実仮想（counterfactual）バックテストを実際に実行し、「1トレードで永久固着」を再現**。修正により二重ポジション化はしないことも確認（17シグナル→17トレード、ドロップ0）
- USD/JPY 2024 の結果（17トレード、PF 0.174、シャープ-2.663、勝率11.8%等）は**独立に再実行してビット単位で一致**、期待値×件数=PnL等の内部整合性も検算一致
- `data/curated/ds-1.json`は監査時点で健全（5通貨×74,232本）、既存テスト11件は変更なしでpass

### 修正が不完全だった点（本追記で対応済み・未対応を明記）

**[重大・本追記で修正済み] B4: データ破損の根本原因が未修正のまま残っていた**
前回の「テスト中にds-1.jsonのUSD_JPYを852本に破損させ、gitで復元した」という事故（前追記参照）の原因ファイル（`data/raw/ds-1/ohlcv_USD_JPY_5min_20240102_20240104.csv`、852行）が**git管理下に残ったまま**だった。`fetch_ds1_ohlcv.py`の集約ロジックは同一通貨ペアの複数CSVを`pairs_data[symbol] = df`で単純に「最後に読んだファイルが勝つ」実装であり、ソート順で当該の壊れたファイルが正規の全期間ファイルより後に来るため、**次回2020-2025データを取得した際に同じ破損が再発する状態**だった。本追記で対応: (1) 破損ファイルを`git rm`、(2) 集約ロジックを「同一シンボルの全CSVを結合してタイムスタンプ重複排除」に変更、(3) 既存`ds-1.json`より件数が減る場合は`--allow-shrink`を明示しない限り書き込みを拒否するガードを追加。修正後、テストで縮小ブロックが正しく機能することを確認済み。

**[重大・本追記で修正済み] B3: `is_jpy_pair`ハードコードが3本目のスクリプトに残っていた**
`scripts/analyze_trades.py`（差し戻し7のSL/TP内訳・保有期間分析に使う予定のスクリプト）にも同じ`is_jpy_pair=True`固定・`spread_pips=0.3`固定が残っていた。`ablation_sweep.py`・`run_backtest_real.py`と同様に修正済み。

**[重大・未対応、次回差し戻し] B1: 差し戻し7の本来の合格基準が未達成**
差し戻し7が本来求めていたのは「SL/TP内訳・保有期間分布が仕様の中期スイング（数日〜数週間）に近づくか」の確認。実際に確認したところ、**修正後もSL比率82.4%（旧83%からほぼ不変）、保有期間は1時間〜2日程度で、依然スキャルピングの域を出ていない**。前追記はこの点を明示していなかった（「Baseプリセット再走行」とだけ記載）。これは合格していない、と明記する。

**[重大・未対応、次回差し戻し] B2: エンジン状態とポジション状態の非連動（潜在バグ）**
`RangeBreakoutEngine`の状態遷移はポジションの実際の建玉状況と無関係に進む。今回のデータセット（保有が短いため）では顕在化しなかったが（17シグナル→17トレードで整合）、保有期間が長くなる設計変更（本来の狙い）をした場合、同方向シグナルがポジション保有中に発生すると黙って破棄される経路がある。次回対応が必要。

**[軽微・未対応]** B5（例外パスでの二重状態遷移リスク）、B6（`spread_round_trip_jpy`が全ペア60円固定のまま）、B7（`margin_usage_pct`が非JPYペアで通貨換算されていない）、B8（`weak_breakout_exclusion_pct`が構造的に常に100%で無意味な指標）、B9（デッドコード）、B10（`evaluate_mtf`が全indicatorをST バー毎に再計算しており非効率、TVT再実行時に大幅な時間増の懸念）。

### ドキュメント記述の訂正

- **論点B（S/R先読み起因説）を撤回**: 前追記は「§3.4のSL83%は先読みバグの影響」と論じたが、修正後もSL比率82.4%とほぼ変化していない。この論点は実測により支持されないため撤回する。
- **「130→17の減少は先読みバグ解消による」という記述を訂正**: 実際には(1)SL/TP先読み解消 (2)確定バーのみスライス (3)エンジン永続化 (4)S/R許容誤差の厳格化(75pips→24pips)、という**独立した4つの変更の複合効果**であり、要因分解は行っていない。特に(4)だけで条件3の通過率が理論上3倍以上厳格化しており、先読み解消**だけ**が主要因とは言えない。
- **「既存テスト11件が全てpassを維持」の位置づけを訂正**: no-regressionの確認であり、修正の妥当性を検証する証拠ではない。`tests/test_strategy.py`は`runner.py`・`simulator.py`を一切importしておらず、**今回修正した5件のバグはいずれも既存テストでは検知できない**(バー間の状態永続化・先読み・確定バースライスを exercise するテストが存在しない)。

### 追記3（2026-08-15）: 差し戻し7の正式記録 — 不合格確定

`scripts/analyze_trades.py`にOBS000007差し戻し7が求める保有期間分布の集計機能を追加し、USD/JPY 2024で実行した（修正後エンジン、`4490a6e`+`bb1416a`時点）。

**保有期間分布（n=17）**:

| 指標 | 値 |
|---|---|
| 中央値 | 6.5 時間 (0.27日) |
| 平均値 | 11.4 時間 (0.48日) |
| 25%-75%分位 | 4.8 - 16.0 時間 |
| 最小-最大 | 1.0 - 47.0 時間 (約2日) |
| 1日以内 / 1-7日 / 7日超 | 16件 / 1件 / 0件 |

**決済内訳**: SL 14件 (82.4%) / TP 2件 (11.8%) / WEEKEND 1件 (5.9%)。連続負け最大13。

**判定: 差し戻し7は不合格確定。** 保有期間中央値6.5時間・94%(16/17)が1日以内であり、spec が定める中期スイング「数日〜数週間」には全く近づいていない。SL比率82.4%も旧実装の83%とほぼ同水準。**先読みバグ・ステートマシン修正は「実装が仕様通りに動くようになった」ことの確認としては完了したが、「戦略が仕様通りの中期スイングとして機能する」ことは全く確認できていない。** この乖離の原因は実装バグではなく、**エントリー条件・利確/損切り設計そのものが短期での決着を前提にした値（ATR倍率ベースのSL/TP）になっている**ことが濃厚（OBS000006の指摘=パラメータが実データ分析に基づいていない、と符合する）。

結果は `research/EXP-FX000001/10-result/trade-analysis-USD_JPY-2024.json` に保存。

### 差し戻し（追加、優先順）

1. ~~[最優先]~~ **完了**: `analyze_trades.py`を実際にUSD/JPYで実行し、SL/TP内訳・保有期間分布を記録。差し戻し7が不合格である旨を正式に記録した（追記3参照）
2. `runner.py`/`simulator.py`に対する回帰テストを新設（先読み・確定バースライス・エンジン永続化・PULLBACK_CONFIRMED両方向遷移・S/R許容誤差・EUR/USD pipスケールを個別に検証するテストケース）
3. B2（エンジン↔ポジション状態の非連動）の対応方針を決定・実装（信号を抑制する or ドロップをカウントして報告する）
4. B6（スプレッドコスト）・B7（証拠金消費率）の通貨別対応
5. B10（indicator再計算の効率化）は2020-2025 TVT再実行前に対応を検討（現状 USD/JPY 1年で約6.5分、5通貨×6年のTVTでは大幅な時間増が見込まれる）

## 追記4（2026-08-15）: 独立監査P0〜P2差し戻しへの対応（B2・B3・B6・B7・回帰テスト）

司令塔から「まとめて優先順位通りに進める」との指示を受け、追記2で挙げた差し戻し(優先順)のうち、以下を実施した。

### 実施内容

| # | 項目 | 対応 |
|---|---|---|
| 1 | (追記3で対応済み) | `analyze_trades.py`実行、差し戻し7不合格を正式記録 |
| 2 | 回帰テスト新設 | `tests/test_backtest_runner.py`を新設(14件)。確定バー特定ロジック(`last_confirmed_bar_ts`をrunner.pyから抽出しテスト可能に)、PULLBACK_CONFIRMED両方向遷移、S/R許容誤差(24pips-75pips境界)、EUR/USD pipスケール、エンジン再利用/process_h4ゲート、SL/TP先読み回帰(反実仮想比較)をそれぞれ検証。既存11件と合わせ計25件、全てpass |
| 3 | B2対応 | エンジン状態とポジション状態の非連動を「カウントして可視化する」方針で対応。`signals_dropped_position_open`カウンタを`BacktestMetrics`に追加し、同方向シグナルがポジション保有中のため破棄された件数を記録するようにした(状態遷移そのものを変更する設計変更は、ステートマシンの意味論に踏み込みすぎるため見送り) |
| 4 | B6対応 | `spread_round_trip_jpy=60.0`の全ペア共通ハードコードを撤廃。`spimulator.spread_round_trip_cost_jpy()`関数を新設し、`SimulatorConfig`の実際の`spread_pips`/`lot_size`/`is_jpy_pair`から動的算出するよう修正 |
| 5 | B7対応 | `margin_usage_pct()`が`is_jpy_pair`引数を受け取りながら未使用だったバグを修正。非JPYペアでは`calc_pnl`と同じ150.0固定レートでJPY換算するよう修正(EUR/USD等で証拠金消費率が実質150分の1に過小評価されていた問題を解消) |

### 見送った項目とその理由

- **B10 (indicator再計算の効率化)**: `evaluate_mtf`がSTバー毎に全indicatorを再計算しており非効率という指摘は妥当だが、修正には`evaluate_mtf`の内部構造(MT/LT計算とST/エントリー判定が一体化している)へのより深い改修が必要で、**本セッションで先読み・ステートマシン等の重要な修正を行ったばかりのコードに、さらにリスクのある変更を重ねることは避けた**。また、TVT再実行自体がGMO週末メンテナンスで現在ブロックされており緊急性が低い。次回、パフォーマンス改善に特化したセッションで対応することを推奨する(具体的な改修方針: `confirmed_h4_ts`/`confirmed_d1_ts`が変化した時だけMT/LT/S-R計算を行うキャッシュ機構を`runner.py`側に追加する)。

### 検証結果

全て`PYTHONPATH=src python3 -m pytest tests/`で25件pass確認。`analyze_trades.py`の再実行結果(`research/EXP-FX000001/10-result/trade-analysis-USD_JPY-2024.json`, `trades-USD_JPY-2024.json`)も保存済み。

### 現在の状態まとめ

OBS000007のブロッキング事項(実装が仕様と乖離)は、判明した主要バグについては修正・検証済みとなった。ただし:

1. **差し戻し7 (保有期間が中期スイング) は不合格確定** — これは実装バグではなく、戦略設計(パラメータ)側の問題である可能性が高い(OBS000006と符合)
2. **差し戻し6 (2020-2025データ取得) は依然未完了** — GMO週末メンテナンス次第
3. B10は次回セッションへ

**総合判定は引き続き「保留」を維持する。** 実装は仕様により忠実になったが、それによって「戦略コンセプトに本当にエッジがあるか」の検証はまだ何も進んでいない — TVTデータ取得 → 修正済みエンジンでの再実行 → permutation test、という一連の作業を経て初めて判断できる。

## 追記5（2026-08-15）: GMO 公開 API のデータ保持期間が spec の前提を満たさないことが判明

差し戻し6(2020-2025データ再取得)の状況を再確認したところ、GMOのステータスは`MAINTENANCE`から`CLOSE`(週末の市場クローズ、メンテナンスではない)に変わっており、平日相当の日付では実際にデータが取得できることを確認した。しかし、**取得できる範囲を調べたところ、公開 klines API の実際のデータ保持期間は現在(2026-08-15)から遡って約2年9か月(2023年10月27日頃以降)までしかない**ことが判明した。

### 確認方法・結果

`GMOClient.get_klines()`をダミー鍵(Public APIのため認証不要)で直接呼び出し、複数の平日日付で挙動を確認した:

| 日付 | 結果 |
|---|---|
| 2023-10-25 以前(2020-06-01, 2022-06-01, 2023-01-03, 2023-06-01, 2023-08-01, 2023-10-01, 2023-10-25 等) | 全て `status=404`(データなし、エラー) |
| 2023-10-27, 2023-10-30, 2023-10-31 | 正常取得(276〜288本、5分足の1日分相当) |
| 2023-11-01 以降(2023-11-15, 2023-12-01, 2024-01-02, 2024-04-01, 2024-08-01, 2024-10-01, 2025-08-01 等、いずれも平日) | 正常取得 |
| 週末・祝日(2024-06-01土, 2024-12-01日, 2025-01-01祝 等) | 0本(データなし、404ではない = 正常な休場扱い) |

404とゼロ件(休場)は明確に異なる挙動であり、**2023-10-25以前は「休場」ではなく「データが存在しない」**ことを示している。境界は2023-10-26/27付近。

### 影響

spec (`research/EXP-FX000001/00-spec.md`) が定める train/validation/test プロトコル(Train 2020-01〜2022-12 / Validation 2023-01〜2024-06 / Test 2024-07〜2025-12)は、**Train期間全体とValidation期間の前半(2023-01〜2023-10頃)がGMOの公開APIから原理的に取得不可能**であるため、そのままでは実行できない。これはこれまで「週末メンテナンスで一時ブロック」と記録してきた内容の訂正でもある — 一時的な障害ではなく、恒久的なデータ提供範囲の制約だった。

これまで実施してきたTVT検証(train/val/test 15セル、OBS000004で参照)も、この制約の中でどう取得されたデータなのか改めて確認が必要(前回の独立監査で「入力データがds-1.jsonに存在しない」と指摘された点と合わせ、TVT結果の再現性・妥当性には引き続き強い留保が必要)。

### 差し戻し(追加)

司令塔の判断が必要な論点として記録し、対応方針の決定を仰ぐ:

1. **train/val/testの期間設計を、実際に取得可能な約2年9か月の範囲(2023-11〜2026-08)に短縮して再設計する** — spec改訂が必要。5年分のバックテストという当初のスコープを大きく縮小することになる
2. **2020-2023年前半の履歴データを別のデータソースから調達する** — GMO以外の提供元(有料データベンダー等)を検討する必要があり、コスト・工数が発生する
3. **現状維持のまま、取得可能な範囲でTVTを実施し、5年分検証という当初目標の未達を明記した上で進める**

**→ 司令塔判断(2026-08-15): 案1(期間短縮再設計)を採用。spec v2.2に改訂し、以下追記6で実施結果を記録する。**

## 追記6(2026-08-15): データ取得完了 + 修正済みエンジンでのTVT正式実行

司令塔判断を受け、以下を実施した。

### 実施内容

1. **5通貨×2023-11-01〜2026-08-15のOHLCVを取得**: `scripts/data/fetch_ds1_ohlcv.py --all-pairs`で実行。722取引日、5通貨×206,133本、合計1,030,665本。全て正常取得(失敗0日)
2. **`data/curated/ds-1.json`(集約後184MB)がGitHubの100MB上限を超過**するため、`.gitignore`に追加して追跡対象外化。個別のraw CSV(各11MB程度、上限内)を正式データソースとしてgit管理し、JSONは`fetch_ds1_ohlcv.aggregate_to_json()`で数十秒で再生成できるようにした。同様に`data/curated/mtf_cache/`(pickle、54MB)も追跡対象外化
3. **`scripts/precompute_mtf.py`でMTFキャッシュを事前計算**(全5通貨で9秒)してから、`scripts/run_train_val_test.py --all-pairs --preset A1_A2_combined`で新期間(spec v2.2)の15セルを実行。総実行時間 6,446秒(約1時間47分)。全セル正常完了、エラーなし

### 全15セル結果

| 通貨 | Train (n=,PF,Sharpe,pass) | Validation | Test |
|---|---|---|---|
| USD_JPY | 18, 0.392, -1.702, 4/9 | 4, 2.678, +1.351, 9/9 | 6, 0.188, -1.340, 5/9 |
| EUR_JPY | 15, 0.804, -0.427, 5/9 | 8, 0.771, -0.536, 5/9 | 3, 0.000, -1.820, 5/9 |
| GBP_JPY | 9, 1.308, +0.302, 7/9 | 5, 0.586, -0.888, 6/9 | 2, inf, +1.225, 8/9 |
| AUD_JPY | 11, 1.535, +0.495, 9/9 | 7, 1.062, +0.090, 7/9 | 7, 0.480, -1.127, 5/9 |
| EUR_USD | 13, 0.278, -1.394, 5/9 | 3, 0.000, -2.744, 5/9 | 1, 0.000, -1.225, 5/9 |

詳細は`research/EXP-FX000001/10-result/train_val_test/tvt_A1_A2_combined.json`。

### 決定的な新事実: サンプル数不足は実装バグではなく戦略設計の帰結

全15セル合計トレード数は**112件**(通貨別: USD_JPY 28 / EUR_JPY 26 / GBP_JPY 16 / AUD_JPY 25 / EUR_USD 17)。年換算すると **約8.1件/通貨/年**(112 ÷ 2.75年 ÷ 5通貨)であり、`00-prescreen.md`が前提としていた「2-3シグナル/月 = 24-36件/年」の**約3〜4分の1**に過ぎない。

5通貨をプールしても、Train期間合計66件はspecの有効サンプル基準(n≥60)をわずかに超えるが、**Validation合計27件・Test合計19件はプールしても届かない**。単一通貨単位では全15セルのどれ一つとしてn≥60に到達しない(最大はUSD_JPY trainのn=18)。

これは`runner.py`の先読みバグ・ステートマシン不持続を修正した**後**に判明した事実であり、実装の問題ではなく、**正しく実装された4条件AND(LT方向 + MT-1レンジブレイク + MT-2 S/Rライン + STプルバック)が、実際の市場でどれだけ稀にしか成立しないかを示す実測値**である。GMOのデータ保持期間制約(追記5)と組み合わさることで、**spec自身が定めた統計的有意性の基準を満たす検証が、現在のデータソースと戦略設計のままでは構造的に実行不可能**という結論に至る。

### 総合判定

**「保留」を維持する。** 実装は仕様に忠実になり、データも実在・再現可能なものに置き換わったが、それでもなお「戦略にエッジがあるか」を判定できるだけのサンプル数が確保できていない。これは追加の実装修正では解決しない(修正すべきバグがもう残っていない)。解決には以下のいずれかが必要:

1. **spec の n≥60 基準の見直し**(通貨ごとから5通貨プールへの読み替え、または基準値自体の緩和)
2. **エントリー条件を緩め、シグナル頻度を上げる方向でのパラメータ再設計**(ただしfalse breakout排除という設計意図と正面から矛盾する)
3. **より長期のデータ確保**(GMO以外のデータソース調達、または今後の運用期間でのデータ蓄積を待つ)
4. **permutation test を小標本のまま実施**し、「有意差なし」という形の結論を正式に出す(エッジの主張を積極的に退ける根拠として使う)

司令塔の判断を仰ぐ。

## 追記7(2026-08-15): プロセス修正(OBS000005差し戻し1・2・5)完了 + permutation testでの全15セル再評価

司令塔の指示（「プロセスを見直して進めてください」）を受け、OBS000005が指摘した構造的欠陥のうち未解消だった3項目に対応した。

### 実施内容

1. **判定エンジン接続(OBS000005差し戻し1)**: `decision/criteria.py`の`evaluate_kpis()`をK1m〜K7m+min_n_trades+permutation_p_valueの全13ゲート評価に拡張(旧実装は7ゲートのみでK5m/K6m欠落)。`run_train_val_test.py`の自前再実装を廃止しこちらに統一。副次的に、`decision/__init__.py`が存在しない関数`decide`をimportしておりREADME記載の動作確認コマンドすら起動不能だった実装バグを発見・修正(判定エンジンが実際には一度も実行されていなかった直接証拠)
2. **改善ループの停止条件明文化(OBS000005差し戻し2)**: spec v2.2→v2.3に改訂。追加試行可能なプリセット数を残り最大3件・Trainのみでの絞り込みと数値で確定。3件消費後もTrain基準未達ならSYS-FX007はパラメータ調整では改善不可能としてREJECT確定する旨、および有効サンプル数n≥60未達論点は試行枠を消費する前に司令塔判断を得るべき旨を明記
3. **permutation test実装(OBS000005差し戻し5)**: `backtest/permutation.py`を新設。specの「ラベルシャッフルでエンジン再実行」(1000回×15セルで計算コスト非現実的)から、確定済みトレード損益列への符号シャッフル検定に方式を簡略化。方式選択の理由・限界はモジュールdocstringに明記
4. 上記の新しい判定エンジン経由で**15セルを再実行**(4119秒)。trade_pnlsをJSON出力に含め、以後の統計再集計をフル再実行なしで行えるようにした

### permutation test結果: 全15セルで有意差なし

| 通貨 | 期間 | n | perm p値 | KPI pass(適用対象/11) |
|---|---|---|---|---|
| USD_JPY | train | 18 | 0.956 | 3/11 |
| USD_JPY | validation | 4 | 0.251 | 9/11 |
| USD_JPY | test | 6 | 0.922 | 4/11 |
| EUR_JPY | train | 15 | 0.588 | 4/11 |
| EUR_JPY | validation | 8 | 0.654 | 4/11 |
| EUR_JPY | test | 3 | 1.000 | 4/11 |
| GBP_JPY | train | 9 | 0.377 | 7/11 |
| GBP_JPY | validation | 5 | 0.669 | 5/11 |
| GBP_JPY | test | 2 | 0.250 | 8/11 |
| AUD_JPY | train | 11 | **0.306** | 9/11 |
| AUD_JPY | validation | 7 | 0.471 | 6/11 |
| AUD_JPY | test | 7 | 0.804 | 4/11 |
| EUR_USD | train | 13 | 0.963 | 4/11 |
| EUR_USD | validation | 3 | 1.000 | 4/11 |
| EUR_USD | test | 1 | 1.000 | 4/11 |

**p<0.05(有意水準)を満たすセルは15セル中0件**。KPI通過数だけを見ると最良に見えるAUD/JPY Train(9/11)ですら p=0.306 であり、コイン投げと統計的に区別できない。トレード数・シャープ・PF・DD・連敗数など、トレードレベルの数値自体はエンジン修正前の追記6と完全に一致しており(同一エンジン・同一データでの再実行のため想定通り)、KPI評価層と統計的検定層のみが新設・更新された。

### 総合判定

**「保留」を維持する。** 実装は仕様に忠実、データは再現可能、KPI評価は判定エンジン経由でK1m〜K7m+統計的有意性を漏れなく評価、統計的検定(permutation test)も実装済み — プロセス上のギャップはほぼ解消した。残るのは「サンプル数不足のため、有意なエッジがある/ないをどちらも断定できない」という統計的事実そのものであり、これは追加のコード修正では解決しない。追記6で提示した4択(基準見直し/パラメータ緩和/データ拡充/小標本permutation test実施)のうち4番目は本追記で実施済みとなったため、選択肢は実質以下3つに絞られる:

1. **spec の n≥60 基準の見直し**
2. **エントリー条件を緩め、シグナル頻度を上げる方向でのパラメータ再設計**(spec v2.3の停止条件に基づき残り3枠のみ、false breakout排除という設計意図とのトレードオフを認識した上で)
3. **より長期のデータ確保**(GMO以外のデータソース調達)

司令塔の判断を仰ぐ。

## 追記8(2026-08-15〜16): count_touches単位不一致バグの発見・修正 + エントリー条件緩和3候補の結果 — 停止条件トリガー

司令塔が対応方針「②エントリー条件緩和」を選択したことを受け、spec v2.4 で候補選定ルールを結果を見る前に確定し、A1_A2_combined を土台に1軸ずつ緩和した3候補（B1_LooseSR: sr_min_touches 3→2 / B2_ShortDonchian: donchian 50→20 / B3_LowerADX: adx閾値 20→15）を Train 期間・5通貨で実行した。

### 新規発見バグ: S/Rライン「接触回数」の許容誤差が100倍広かった

集計中、B1_LooseSR（sr_min_touches 緩和）が5通貨全てでベースラインと**トレード数・シャープ・PFが完全一致**するという異常に気づき調査したところ、`support_resistance.py` の `count_touches()` に単位不一致バグを発見した。

- `cluster_fractals()` は `cluster_threshold_pct=0.5` を「0.5%」として正しく処理（内部で `/100`）
- `count_touches()` は `tolerance_pct` を「比率」（0.005=0.5%）で受け取る関数だが、`detect_support_resistance()` が `touch_tolerance_pct=0.5`（cluster_threshold_pct と同じ「%数値」規約）を `/100` せず直接渡していた

USD/JPY Train期間で実測したところ、本来なら数回のはずの「接触回数」が全32本のS/Rラインで一律**1609回**（期間内バー数の約73%）という明らかに壊れた値になっていた。`min_touches` フィルタが実質無効化され、`compute_strength()` の接触スコアも `touch_score = min(1.0, touches/5.0)` で常に満点(1.0)に張り付いていた。

**修正**: `detect_support_resistance()` の呼び出し箇所で `touch_tolerance_pct / 100.0` に変換（`count_touches()` 自体の規約・既存テストは変更なし）。修正後の実測: touches分布が1609固定→3〜319の妥当な範囲に。回帰テスト2件を追加（42/42 pass）。

この修正はMT-2ゲーティングに影響しうるため、ベースライン（A1_A2_combined）を含む主要TVTを全て再実行した。**結果、ベースラインのTrainプール値（n=66, perm_p=0.9316）は修正前と完全に一致**——実測の接触回数（3〜319）の大半が `touch_score` の飽和点（5回）を超えていたため、スコア自体は歪んでいたが飽和後の値（1.0）は変わらず、少なくともこのデータセットでは意思決定への影響がなかったことを確認した。バグの実在と修正の必要性自体は変わらない（正確性の問題として独立に修正価値がある）。

### エントリー条件緩和3候補の結果（spec v2.4 選定ルール適用）

修正済みコードで3候補を Train・5通貨プールで再評価した。

| 候補 | プール n_trades | 各通貨 sharpe (参考) | pooled perm p値 | 選定対象 |
|---|---|---|---|---|
| ベースライン(A1_A2_combined) | 66 | — | 0.9316 | (基準) |
| B1_LooseSR | 66 | 変化なし(全通貨で完全一致) | 0.9336 | **NO** |
| B2_ShortDonchian | 90 | AUD +0.676 / EUR_JPY +0.077 / 他負 | 0.8864 | **NO** |
| B3_LowerADX | 86 | GBP +1.232(PF 2.46) / AUD +0.876(PF 1.71) / 他負 | 0.5071 | **NO** |

選定基準（n_trades≥66 かつ perm_p<0.05）を満たす候補は**0件**。B3_LowerADXはp値がベースラインの半分近く(0.507)まで改善し、GBP/JPY・AUD/JPY単体では良好な数値(GBP: sharpe+1.232, PF2.46, perm_p=0.106、AUD: sharpe+0.876, PF1.71, perm_p=0.238)を示したが、選定ルールの閾値には届かなかった。

### 停止条件トリガー

spec v2.3「今後の試行回数上限」項目3に従い、**残り試行枠3件を使い切り、Train基準を満たす候補が0件**となった。同項目の規定により、**SYS-FX007（現行のLT+MT-1+MT-2+ST 4条件AND構造）はパラメータ調整では改善不可能と判断しREJECT確定**とする条件に該当する。次の一手は同一構造でのパラメータ再探索ではなく、エントリー条件の構造そのものの見直し（SYS-FX008等の別戦略カテゴリへの転換）とすることが、事前に確定したルールの帰結である。

Validation/Testへは（選定対象の候補が0件のため）進まない。

### 総合判定

**「保留」を維持しつつ、事前登録した停止条件がトリガーされたことを明記する。** この結論はHARKingを避けるため結果を見る前に確定したルールの機械的な適用であり、恣意的な判断ではない。ただし「REJECT確定」を実際に採用するかどうかの最終判断は司令塔に委ねる（B3_LowerADXの一部通貨での改善傾向をどう解釈するか、SYS-FX008への転換を今すぐ行うか、を含む）。

> **⚠️ 2026-08-16 追記（OBS000005追記4参照）**: 本結論は、Phase 1（DATA-FX001、特にDS-7スワップが未接続のままスワップ0円固定で全TVTが実行されている）とパラメータ導出（OBS000006 Phase 0-5、現行パラメータは全て教科書的慣例値でエントリー条件緩和探索も同様）の**2つのゲートが未クリアのまま得られた暫定結論**である。`research/ACTIVE.md` にフェーズゲートを新設し、ゲートクリア（またはゲートを明示的にスキップする司令塔判断）まで、本結論を最終確定として扱わない運用とする。
>
> **⚠️ 2026-08-16 追記2（ゲートクリア後の結論）**: 上記2ゲートはその後クリアした（ゲート1: DS-4/DS-7対応・DS-2/DS-3代用値受入、ゲート2: OBS000006 Phase 1・2でトレンド強度軸・SL/TP/Donchian軸をデータ駆動で検証）。ゲート2の検証で、本PJ初めて十分なサンプル数（pooled n=125）を確保した上で明確な非有意（perm_p=0.80）という結果が得られ、OBS000006はC品質チーム視点の最終判定を保留から不採用に更新した（OBS000006追記7）。本結論（REJECT相当）は、ゲートクリア後の確定結論として扱ってよい状態になった。SYS-FX007を正式にREJECT確定するかどうかの最終判断は司令塔に委ねる。

## 変更履歴2
- 2026-08-15: 差し戻し 1〜5・7(一部) の修正実施結果を追記。SL/TP先読み・MT/LT先読み・ステートマシン永続化・S/R許容誤差・is_jpy_pairハードコードを修正。修正過程で新規発見した PULLBACK_CONFIRMED 状態固着バグも修正。Base プリセット再走行 (USD/JPY 2024) で n_trades 130→17 (先読み解消) を確認したが、依然 PF<1・シャープ<0 で結論は保留のまま。差し戻し6 (データ再取得) はGMO週末メンテナンスにより一時ブロック、次回平日に再試行予定
- 2026-08-15: 1時間毎自動監視ルーティンによる独立Opus再監査結果を追記。5件の主要修正は実データ検証で裏付け確認。データ破損の根本原因(B4)と3本目のis_jpy_pairハードコード(B3)を追加修正。差し戻し7の本来の合格基準(保有期間)は未達成であることを明記、論点Bを撤回、「130→17は先読み解消による」という記述を「4変更の複合効果」に訂正
- 2026-08-15: `analyze_trades.py`に保有期間分布集計を追加しUSD/JPY 2024で実行。差し戻し7を正式に**不合格確定**(保有期間中央値6.5時間、94%が1日以内、SL比率82.4%)。原因はエントリー/決済パラメータが短期決着前提の値になっている可能性が高く、OBS000006の指摘と符合すると追記
- 2026-08-15: 独立監査差し戻し(優先順)への対応を追記。回帰テスト新設(25件)、B2(エンジン↔ポジション非連動をカウント方式で可視化)、B6(スプレッドコストの動的算出化)、B7(証拠金消費率のJPY換算修正)を実施。B10(indicator再計算の効率化)はリスク・緊急性を勘案し次回セッションへ見送り。総合判定は引き続き保留を維持
- 2026-08-15: GMO公開klines APIの実データ保持期間を直接検証した結果、2023年10月26/27日頃以前のデータが原理的に取得不可能(404)と判明。specのtrain/val/testプロトコル(Train 2020-2022〜)が現行データソースでは実行不可能であることを追記。「週末メンテナンスで一時ブロック」としてきた過去の記録を訂正。司令塔判断を仰ぐ3つの対応案を提示
- 2026-08-15: 司令塔判断(期間短縮再設計)を受け、5通貨×2023-11〜2026-08のOHLCV取得(722取引日、1,030,665本)とTVT15セル正式実行が完了。全15セルでn≥60未達(最大n=18)と判明、年換算シグナル頻度が実測で約8.1件/通貨/年、spec前提の1/3〜1/4に過ぎないことを発見。実装バグではなく戦略設計の帰結と結論づけ、判定は保留を維持。4つの解決オプション(基準見直し/パラメータ緩和/データ拡充/小標本permutation test)を提示し司令塔判断を仰ぐ
- 2026-08-16: 司令塔選択「②エントリー条件緩和」への対応中、count_touches()の単位不一致バグ(接触回数が100倍=1609回に膨張)を新規発見・修正。ベースライン再実行では影響なしと確認。緩和3候補(B1/B2/B3)をspec v2.4の事前登録ルールで評価した結果、選定対象0件。spec v2.3の停止条件がトリガーされ、SYS-FX007は現行4条件AND構造ではパラメータ調整による改善が困難という機械的な結論に達した。最終判断は司令塔に仰ぐ
- 2026-08-16: 上記結論がPhase 1（DATA-FX001）未受入・パラメータ導出（OBS000006）未実施の2ゲート未クリアのまま得られたものと判明（OBS000005追記4）。ACTIVE.mdにフェーズゲートを新設したことを受け、本結論を「ゲートクリア前の暫定結論」と明記する注記を追加

## 変更履歴
- 2026-08-14: 作成（独立 Opus エージェントによる OBS000003〜006 の再監査結果を受け、バックテスト実装自体の正当性を検証する adversarial review として起票。最重要4件は本セッションでソースコード・データを直接確認して再現。判定: 保留・ブロッキング）
