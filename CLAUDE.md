# Claude Code Instructions

このプロジェクトは**FX（外国為替証拠金取引）の自動売買戦略を、マルチタイムフレームで定量的に検証する**「minmax-fx-day-trading-lab」プロジェクトです。

「minmax」は minmax チーム（ユーザー）のクオンツ検証プロジェクト群の**共通識別子プレフィクス**で、GitHub リポジトリ名は `minmax-<topic>` の形式（例: `minmax-trading-pilot`, `minmax-fx-day-trading-lab`）で揃える。`minmax-trading-pilot` は FX 自動売買システム構築の親プロジェクト、本PJはその検証ラボ（バックテスト・フォワードテストの定量評価レイヤ）として位置付ける。

lp-strategy-lab（流動性プール戦略）および trading-app-v2（BTC キャリ実運用）とは別に新規構築する、**FX マルチタイムフレームのクオンツ検証ラボ**です。

## 開発言語 / フレームワーク

**Python 3.11+ エコシステム**で構築（**親PJ `minmax-trading-pilot` から派生**）。

主要依存:
- **データ**: `pandas`, `numpy`, `requests` (GMO FX API 取得)
- **指標**: `pandas-ta`
- **分析**: `scipy`, `pandas`
- **バックテスト**: `backtrader`, `vectorbt`, `optuna`（optional）
- **ログ**: `structlog`, `python-dotenv`
- **品質**: `pytest`, `ruff`, `mypy`, `black`（dev）

親PJからの流用資産（`src/minmax_fx_dt/` 配下）:
- `data/gmo_fx_client.py` — GMO Coin 外国為替FX API クライアント（HMAC-SHA256 認証、klines / ticker / symbols / order 全エンドポイント）
- `risk/sizing.py` — ポジションサイズ決定（親PJ踏襲 + 本PJ向けに SYS-FX007 用に拡張）
- `decision/criteria.py` — 撤退/採用判定（K1m〜K7m 7 指標で評価、親PJの単一 Sharpe から拡張）
- `notify/discord.py` — Discord Webhook 通知

将来の TS/Node 採用は親PJ v2 との並走領域（ダッシュボード等）に限定。

> **スコープ方針変更（2026-08-13）**: 当初「デイトレード縛り（D1 未満、当日クローズ必須）」で起票したが、司令塔からの中期スイング戦略方針（両建て運用あり・長期方向 + 中期レンジ・ブレイク後の戻りエントリー）に合わせて、**スコープを「マルチタイムフレーム・中期スイング」に拡張**。M1〜H1 の短期足はエントリータイミング用に保持しつつ、D1/W1 の長期足もスコープに含める。週末持ち越しは引き続き対象外（FX 祝日・週跨ぎで再評価）。

## 検証対象のスコープ

- **対象市場**: 外国為替（FX、現物/差金決済/CFD いずれも扱うが主軸は CFD 方式）
- **対象時間軸**: マルチタイムフレーム
  - **LT（長期方向）**: D1 / W1 — トレンド方向の判定
  - **MT（中期レンジ）**: H4 / H1 — レンジ上限・下限の決定
  - **ST（エントリー）**: M15 / M5 — 戻り確認の執行タイミング
  - ホールド期間: **数日〜数週間**（中期スイング）
  - 週末持ち越し: **原則不可**（土曜 06:00 JST までに全決済）。戦略の構造上必要な場合のみ、司令塔の明示判断＋リスク対策（窓開け約定モデル・週末エクスポージャー上限・最大保有期間・証拠金維持率ロスカット）の実装を条件に個別許可（2026-08-28、SYS-FX024 で初適用。詳細は `PJ000004` Q7 および `research/EXP-FX000018/00-spec-amendment-01.md` §3）
- **対象通貨ペア**: **最大 5 通貨**（OBS000002 で選定、暫定案 A: USD/JPY, EUR/JPY, GBP/JPY, AUD/JPY, EUR/USD）
- **戦略カテゴリ**: レンジブレイク・プルバック（OBS000001）、両建て・グリッド系、ボラティリティ・ブレイク、ニュース窓トレード、長期方向フィルター合成
- **対象外**:
  - 暗号資産の FX 派生（USD-M 等）。GMO コインの暗号資産 API 拡張は将来余地として残すが本PJの主軸ではない
  - ポジショントレード（数ヶ月単位の長期保有）
  - マクロ経済学的な長期テーマの裁定
  - 流動性プール（lp-strategy-lab 側のスコープ）
  - 純裁量トレードの記録・再現

## プロジェクトの優先順位

- 「FX マルチタイムフレームで再現性のあるエッジが存在するか」を機械的に検証し、**優位な戦略のみを本実装（trading-app-v2 と分業または共有の実行基盤）へ送る**プロセスそのものがゴール
- 戦略カテゴリごとに**5年バックテスト・6ヶ月フォワードテスト**の精度を上げ、複数戦略を並列に比較できる共通 KPI（Sharpe / 最大DD / 勝率 / ペイオフレシオ / Profit Factor / 月次期待値 など）で評価する
- 採用された戦略は、`trading-app-v2` 系の実行基盤（または本PJ内に新設する execution モジュール）へ接続し、**GMO コイン外国為替FX API**（API 手数料 = 約定金額 × 0.002%、USD/JPY スプレッド0.3銭、レバレッジ最大25倍）経由で実運用する
- **両建て運用に対応**する。同時両建てポジションの証拠金消費と PnL 推移を内部で監視する
- **スコープは「マルチタイムフレーム・中期スイング」**。短期ノイズ足のスキャルピングは対象外

## 開発の方針
- 日本語で回答する。途中で英語になることがあるため注意
- セキュリティを優先する
- API キー・ `.env.local` は読まない
- 変更は影響範囲を確認
- 不明瞭な要求は、勝手に対応するのではなく要件確認
- **同じ内容の問い合わせループを禁止する**。同一の確認事項を表現を変えて繰り返し尋ねない。不明点は一度にまとめて確認し、回答を得たら実行に移る
- 検証 → 評価 → 修正 → テスト → デバッグのサイクルを基本とする
- 他リポジトリのリソースアクセスは原則不可（本PJは独立）
- ブローカー接続・実発注は検証済戦略のみ（採用GOが出てから着手する）

## マルチエージェント体制

lp-strategy-lab で確立した 6体のマルチエージェント体制を踏襲する：

- **S 戦略チーム (chief-strategist)**: FX 市場の大局観・**戦略間の期待値/優先順位付け**・全体監視・司令塔への具申
- **A 設計チーム (strategy-architect)**: 検証仕様（spec）確定。評価基準を**試算前に数値で**固定
- **B 実装チーム (quant-researcher)**: spec に忠実に実装。Tick/分足レベル + ブローカー API 連携の**生データのみ**出力（評価はしない）
- **C 品質チーム (adversarial-reviewer)**: 良好結果を疑い倒す、楽観バイアスの最終砦。採用/不採用を断じる
- **D デプロイチーム (integration-deploy)**: 採用後、実行基盤（execution モジュールまたは trading-app-v2）への組み込み
- **E 進行チーム (archivist-pm)**: OBS 移動・引き継ぎ更新・ACTIVE 更新・採用台帳管理

採用判断の権限は人間（司令塔 = ユーザー）に属する。C の「採用可」は参考意見であり、本番判定（D の起動）は**司令塔の明示GO**を必須とする。

## マルチタイムフレーム戦略ラボ固有の検証ステップ（LP 戦略ラボとの違い）

LP 戦略ラボの「レンジ幅 × 期間 × チェーン」を 4次元探索したのに対し、本PJは

1. **戦略カテゴリ × 通貨ペア × 時間軸階層 × パラメータ空間** の 4次元
2. **マルチタイムフレーム OHLCV + スプレッド** を必須データに追加
3. **時間制約**: ホールド期間は**数日〜数週間**、週末持ち越しは原則禁止（個別許可制、上記参照）、最大同時両建てポジション数の制約あり
4. **執行モデル**: 指値（戻り確認）と逆指値（ブレイク確認）のハイブリッド、**GMO コインの実スプレッド**を反映（USD/JPY 0.3 銭、GBP/JPY 0.7 銭 等）
5. **コスト構造**: API 手数料 = 約定金額 × 0.002%（30日間無料トライアル中は実質ゼロ）、スプレッド 通貨ペア別、スリッページ 0.5 pip、**スワップポイントは両建て時にロング・ショート両側で発生**するため正味で計算
6. **リスク制約**: 1ヶ月あたりの最大DD、年間最大DD、最大連続損失トレード数、両建ての証拠金消費率、通貨ペア間相関、などを厳格にKPI化

LP 戦略ラボの「HARKing 防止（試算前に評価基準を数値で固定）」は踏襲し、本PJ 固有の HARKing リスクとして「スワップポイント無視」「両建ての証拠金消費過小評価」「経済指標時刻のスリッページ急増」に対する品質ゲート Q1〜Q9 を DATA フェーズで適用する。

## KPI の暫定定義（spec 確定前にプレースホルダ）

- **K1m**: 月次 Profit Factor > 1.2、**月次シャープレシオ ≥ 0.4**、月次期待値 > 0
- **K2m**: 最大DD（月間） ≤ 証拠金の 10%、**最大DD（年間） ≤ 証拠金の 20%**
- **K3m**: 最大連続損失 ≤ 5 トレード
- **K4m**: ペイオフレシオ ≥ 1.5（= 平均利益 ≥ 平均損失 × 1.5）
- **K5m**: 1トレードあたり期待値 > スプレッド往復コスト × 3
- **K6m**: バックテストとフォワードテストの KPI 乖離率 ≤ 30%
- **K7m**: 両建て状態の最大証拠金消費率 ≤ 30%（証拠金に対する比率）

これらは `STRATEGY-BRIEF.md` で確定し、戦略 SYS-FX001〜SYS-FX??? ごとに spec へ転記する。

## コードインライン
- イン前に影響範囲を確認
- 不明瞭な要求は、勝手に対応するのではなく要件確認
- 既存実装と整合しない箇所があれば先にチェック
- 不具合があれば似たような不具合が無いかチェック
- 仕様にない要件は確認してから実装
- 既存処理の仕様を変える場合は、理由とともにその場で明記
- 必要に応じてリファクタリングしてもよい
- スマホで操作することもあるため、CLI 操作の可逆性に注意
- 「ブラウザで DevTools を開いて〜」のようなユーザー操作前提の指示は出さない

### プロジェクト進捗サマリの定期更新
- `obs/minmax_fx_day_trading_lab/00プロジェクト方針/PJ000003-プロジェクト進捗サマリ.md` を**週次（週末）または重要変更時**に更新する。
- 更新内容: ゴールまでの距離（KPI 現在地）・戦略ごとの状態サマリ・直近マイルストーン・ブロッカー・次のアクション。
- セッション開始時に `research/ACTIVE.md` と `research/STRATEGY-BRIEF.md` を読んだ後、PJ000003 を確認して文脈を継続する。
- 更新のトリガー:
  - 週次の決まったタイミング
  - C 品質チームの採用/不採用判定
  - フォワード検証の途中チェックポイント（30日/60日/90日）
  - 新規検証（EXP-FXxxxxx）の起票または完了
  - 新規戦略（SYS-FXxxx）の起票
  - 司令塔から「進捗まとめて」と指示があったとき

### ドキュメントの系

検証ログと意思決定の記録は `obs/minmax_fx_day_trading_lab/` 配下に Obsidian 形式で保存する。プロジェクト全体方針は `00プロジェクト方針/`、アイデアは `01開発アイデア/`、対応検討中は `02対応検討中/`、対応待ちの TODO は `70対応待ち/`、採用は `80採用/`、不採用は `90不採用/`、外部レビューは `85外部レビュー/`。引き継ぎノートは `引き継ぎ/01進行中`（セッション最初）と `02済み`（過去）。

各開発ノート（OBS）は `OBSxxxxxx-タイトル.md`（6桁連番、ファイル名昇順で古い順）。`引き継ぎ/` には日次で `YYYYMMDD-引き継ぎ.md`。

## 全プロジェクトルール適用時の TODO 運用
- **全てのプロジェクトルール適用**、および**引き継ぎ資料**・**外部レビュー結果**への対応は、着手前にまず件名単位で TODO に分割する
- 各 TODO は完了の都度クローズする（まとめて後追いクローズしない）

### ビルド/リソース管理
- 検証コードは GitHub 管理（個人開発のため main ブランチへ直接コミットでよい）
- 自動テスト・CI は将来導入を検討（現時点ではローカル検証で十分）
- ブローカー API との接続は **research 段階では GMO の test/paper 環境があれば優先利用、なければ取得 OHLC のオフライントレーニングで開始**

### 他プロジェクトとの関係
- 本プロジェクトは lp-strategy-lab / trading-app-v2 とは別のリポジトリ・PJ として独立運用する
- trading-app-v2 は BTC キャリ実運用。FX 戦略を採用に導く経路は PJ 後半で確定（PJ000001 §6 を参照）
- 採用された戦略を trading-app-v2 の FX モジュールへ統合する経路は **PJ000001 §6 完了後に決定**
- crypto-strategy-lab は多戦略の探索的検証、K線トレード/チャート分析/スイング系の戦略はこちらではなく crypto-strategy-lab 側のスコープ
- FX 市場の規制・税制・ブローカースペックは頻繁に変わるため、四半期に一度 PJ000003 で再評価
