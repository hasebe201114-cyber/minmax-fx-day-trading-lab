# ACTIVE - 進行状況の信号機

> 担当: E 進行チーム（archivist-pm）が更新する**機械的な進行状況**。
> 「今どの検証が、誰の手にあり、次は誰待ちか」を 1 画面で表す。
> 戦略的な勝敗判断・次の一手・戦略カテゴリ間の優先順位は `STRATEGY-BRIEF.md` を参照。

## 現在進行中の実験

| 実験 ID | テーマ | 担当 | ステップ | 状態 |
|---|---|---|---|---|
| [EXP-FX000001](/research/EXP-FX000001/00-prescreen.md) | SYS-FX007 v2（レンジブレイク・プルバック）: 5 通貨 × 3 レイヤ MTF × 5 年バックテスト。**エントリー 5 条件 AND**（LT 方向 + MT レンジブレイク + S/R ライン + 注文板/センチメント + ST 戻り確認） | **A 設計チーム** | prescreen=GO, spec=確定（v2 拡張）、**DATA-FX001 受入待ち → B 実装着手** | 提案デフォルト 4 件で固定済み。K1m〜K7m 評価基準を数値確定。`--mode full|fallback` の 2 モード評価 |
| [DATA-FX001](/research/DATA-FX001/00-data-spec.md) | DS-1(OHLCV) / DS-2(スプレッド) / DS-3(経済指標) / DS-4(セッション) / DS-5(スリッページ) / DS-6(API 仕様) / DS-7(スワップ) / **DS-8(注文板/センチメント、v2 新規)** の取得・検証 | **B 実装チーム** | spec 確定（v2 拡張）、**取得着手待ち** | DS-6 で DS-8 取得可否（パターン A/B/C）を確認後、DS-1 OHLCV と並列で DS-8 着手。Q10（注文板連続性）追加 |

## 直近の状態
- 2026-08-13: プロジェクト新規起票
- 2026-08-13: 戦略方針変更 — デイトレード縛りを解除しマルチタイムフレーム・中期スイング + 両建てに拡張
- 2026-08-13: 司令塔「進めてください」で 4 件 GO。EXP-FX000001 (prescreen/spec) と DATA-FX001 (data-spec) を起票
- 2026-08-13: **v2 拡張** — 司令塔追加要件「注文板 + レジサポラインの判定」を MT サブ条件として統合。DS-8 追加。エントリー 3→5 条件 AND

## 次のアクション

### A. DATA-FX001（B 実装チーム・最優先）
- **DS-6**（GMO API 仕様）取得 → **DS-8 取得可否（パターン A: top-of-book + センチメント / B: top-of-book のみ / C: top-of-book のみ＋MT-3 フォールバック）を確認**
- **DS-1**（5 通貨 × 5 年 M5 OHLCV）取得 → `data/curated/ds-1.json` + **S/R ライン派生 → `data/curated/ds-1-sr-lines.json`**
- **DS-2**（スプレッド時系列）取得
- **DS-3**（経済指標カレンダー）取得
- **DS-4**（セッション境界）: DS-1 から自前派生
- **DS-5**（スリッページ）: 0.5 pip フォールバック
- **DS-7**（スワップポイント日次）取得
- **DS-8**（注文板 / センチメント）: DS-6 で確認したパターンで取得
- 検証スクリプト `scripts/data/verify-ds.ts` を実装し Q1〜Q10 を実行
- `data/MANIFEST.json` に全データ系列のメタ情報を記録
- C 品質チーム DATA モードが受入検査

### B. EXP-FX000001（DATA 受入後着手）
- **B 実装チーム**: `scripts/exp/exp-fx000001.ts` を実装
  - パラメータ空間: 約 800 万通り（spec §パラメータ空間参照）
  - 段階的絞り込み: train で上位 0.1% → validation で 50 候補 → test で 10 候補
  - permutation test 1000 回
  - **S/R ライン検出ライブラリ**: `scripts/exp/lib/detect-support-resistance.ts`
  - **2 モード評価**: `--mode full` (MT-1/MT-2/MT-3 すべて必須) と `--mode fallback` (MT-3 スキップ)
- 出力: `10-result/prediction-unit.json`, `pipeline.json`, `params.json`, `run.log`
- C 品質チームが adversarial review で K1m〜K7m 適合を判定

### C. 検証フェーズ移行
- DATA-FX001 受入 → バックテスト実行 → C 判定 → 司令塔 GO → フォワードテスト
- フォワードテスト: 6 ヶ月（30/60/90 日チェックポイント）、ペーパートレード優先

## ブロッカー
- **DS-6 取得時に GMO の注文板 / センチメント API の有無を確定する必要あり**（パターン A/B/C の判定）

---

## 検証フェーズの運用方針（2026-08-13・方針確定）

### Phase 1: データ基盤（DATA-FX チケット）
- 目的: DS-1〜DS-8 を取得・検証・カタログ化（PJ000004 §3 参照）
- 完了条件: 全 Q1〜Q10 合格

### Phase 2: 戦略 prescreen
- 目的: SYSTEMS.md の各 SYS について GO/PAUSE を S が断じる
- **EXP-FX000001（SYS-FX007 v2）は GO 確定済み**

### Phase 3: 詳細 spec 確定
- 目的: K1m〜K7m の閾値を**バックテスト結果を見る前に**固定
- **EXP-FX000001 の spec v2 確定済み（00-spec.md）**

### Phase 4: バックテスト → フォワードテスト → 採用判定
- 目的: 5 年バックテスト + 6 ヶ月フォワードテスト
- 開始条件: DATA-FX001 受入 + B 実装着手

### 並列検証の意義
- 5 通貨 × 3 レイヤ MTF × 10+ パラメータ次元 = 高次元探索だが、通貨と時間が独立なので並列化可能
- 通貨ポートフォリオ合成で個別通貨のテールリスクを分散
- レンジ相場では両建てで収益、トレンド相場では強いブレイク（5 条件 AND）で勝者側を追従
