# 検証仕様書（Spec） - EXP-FX000006

> 担当: A 設計チーム（strategy-architect）
> 起票: 2026-08-22
> 前提: `00-prescreen.md`（GO）
> 原則: 評価基準は**試算前**に数値で確定する（HARKing防止）。以後の変更は`00-spec-amendment-NN.md`として積む（T-17の運用ルールを本EXPにも適用）

## 対応 OBS 番号
（E進行チームが採番予定）

## 紐づく戦略系統 ID
SYS-FX012

## 仮説（1行）

**H1のボラティリティ・ブレイク（単発の値幅急変）だけでなく、「H1が構造的にトレンド継続している」という確認（Donchianブレイク等）を検出層に追加することで、SYS-FX011が抱える実効サンプル数不足を、ボラティリティの最低水準フィルターと併用することでトレード当たりの質を大きく損なわずに解消できるのではないか。**

## 検出層の設計（事前登録、結果を見る前に固定）

`00-prescreen.md`の事前診断（診断1: 再開エントリーの質的優位性、診断2: Donchian完全置換は質を悪化させる）を踏まえ、**既存のN_BREAKOUT検出を完全に置き換えるのではなく、追加条件として合成する**設計とする。

H1バー`i`について、以下の**OR条件**でトレンドイベントを検出する（`i`が同時にUP/DOWN両方の条件を満たすことはない。判定順序は明記の通りUP→DOWNの対称ロジック）:

```
range_atr_ratio[i] = (high[i] - low[i]) / ATR(H1, 14, Wilder)[i]
DCU[i], DCL[i] = Donchian(20)（bars[i-20:i]、バーi自身は含まない。indicators.pyのdonchian()既定値をそのまま使用、shift(1)相当）

UP条件  = (range_atr_ratio[i] >= N_BREAKOUT かつ close[i] > open[i])
       OR (close[i] > DCU[i] かつ range_atr_ratio[i] >= CALM_RATIO)

DOWN条件 = (range_atr_ratio[i] >= N_BREAKOUT かつ close[i] < open[i])
       OR (close[i] < DCL[i] かつ range_atr_ratio[i] >= CALM_RATIO)
```

- `N_BREAKOUT = 3.5`（SYS-FX011のTrain導出値をそのまま流用、**再導出しない**）
- `CALM_RATIO = 2.0`（`price_shock_filter.py`で既に確立済みの定数をそのまま流用。「静穏バー」の閾値を「継続イベントとして受理する最低ボラティリティ」に転用する。**本EXP用に新規調整する自由パラメータではない**）
- `DONCHIAN_LENGTH = 20`（`indicators.py`の`donchian()`既定値をそのまま流用）

**この設計により新規に導入する自由パラメータはゼロ**（すべて既存コードベースで確立済みの定数の組み合わせ）。これは診断2の「単純な検出層置き換えは質を悪化させる」という負の所見を踏まえ、(a) N_BREAKOUT単独で捉えていた既知の良質な母集団をそのまま温存し、(b) 継続シグナル（Donchian）による追加候補には「静穏でない（CALM_RATIO以上）」という質フィルターを課すことで、診断2で観測された希釈を抑制する狙い。

イベント検出後は`select_non_overlapping_breakout_events()`（重複除去、72h窓、SYS-FX011と同一）を適用する。

## 検証範囲・既存資産の再利用（変更しない部分）

**検出層のみを変更し、それ以外はSYS-FX011の最終候補（trailonly版、T-01〜T-13適用後）と完全に同一のロジックをそのまま使う**（1変数のみを動かす、という改善ループの原則を起票段階から適用）:

- 対象通貨: 4通貨（USD/JPY・EUR/JPY・GBP/JPY・AUD/JPY）
- Train/Validation/Test期間: SYS-FX011と同一（Train: 2023-11-01〜2025-03-31 / Validation: 2025-04-01〜2025-11-30 / Test: 2025-12-01〜2026-08-15、Testは凍結扱いで参照のみ）
- エントリー層（M5ダウ理論連続追跡）: `zigzag_threshold_atr_m5 = 1.0`（変更しない）
- ストップ: `stop_buffer_atr_m5 = 0.703`（SYS-FX011のTrain導出値をそのまま流用、**再導出しない**）
- 出口: トレール専業（T-13、`tp_levels=[]`・`breakeven_trigger_r=1.0`・`atr_trail_multiplier_m5 = stop_buffer_atr_m5 × 1.0`）
- コストモデル: T-09確定版（スプレッド通貨別・スリッページ市場成行0.5pip/逆指値1.0pip・手数料0.00004）
- 価格反応型ショック抑制フィルター: SYS-FX011のv2設計（既存のN_BREAKOUT=3.5をそのまま流用、複数通貨同時ブレイク検知）をそのまま適用
- ホールド期間: 週末持ち越し禁止（本PJ共通ルール）

## KPI閾値（SYS-FX011と完全に同一枠組みを流用、結果を見る前に固定）

| KPI | 閾値 | 必須/参考 |
|---|---|---|
| K1m 月次シャープ | ≥ 0.4 | 必須 |
| K1m PF | ≥ 1.2 | 必須 |
| K1m 月次期待値 | > 0円 | 必須 |
| K2m 月間DD（ピーク比、複利サイジングのためQ9準拠） | ≤ 10% | 必須 |
| K2m 年間DD（ピーク比） | ≤ 20% | 必須 |
| K3m 最大連続損失 | i.i.d.帰無分布パーセンタイル判定（`compute_k3m_scale_invariant()`） | 参考 |
| K4m ペイオフレシオ | ≥ 1.5 | 必須 |
| K5m スプレッドコスト倍率 | ≥ 3倍 | 必須 |
| min_n_trades（実効値、`compute_n_trades_effective()`、相関補正あり） | ≥ 300 | 必須 |
| permutation_p_value（`permutation_test_block()`、エントリー日ブロック順列） | < 0.05 | 必須 |

必須9項目・参考1項目（K3m）という構成もSYS-FX011のT-11ルールをそのまま踏襲する。

## 改善ループの停止条件（`PJ000004`「Q10」準拠、新規EXPとして初適用）

- **改善ループ上限: 5回**。上限に達し、かつ必須KPIゲート（上記9項目）が未達のままの場合、自動的にREJECT確定とする
- カウント方法はT-18の数え方に準拠（結果を見て採否/設計変更を決めた試行のみをカウント。バグ修正・検定手法自体の是正は含めない）
- 本spec（検出層のOR合成設計）自体は起票段階の設計であり、改善ループの試行としてはカウントしない（Trainベースライン評価そのもの）

## フェーズゲート状況

| ゲート | 状態 |
|---|---|
| ゲート1: データ基盤 | ✅ SYS-FX011と同一データ（DS-1、M5 OHLCV、4通貨）でそのまま対応可能。新規データ取得不要 |
| ゲート2: パラメータ導出 | ✅ 新規に導出すべき自由パラメータなし（検出層の3定数、M5層の全パラメータともSYS-FX011からの流用のみ） |
| Trainベースライン評価 | 未実施（次のアクション） |

## 検証プロトコル（過学習検証、SYS-FX011と同一の運用）

- Train単独で選定基準を満たすかをまず確認する。満たさない場合は司令塔判断を仰ぎ、機械的REJECTとはしない（ただし改善ループ上限5回は適用）
- Validationは、Trainで一定の見込みが立った場合にのみ参照する（HARKing防止）
- Testは参照のみとし、意思決定には用いない（本PJ標準のTest凍結方針を起票段階から適用）

## Trainベースライン評価（改善ループ第1試行、2026-08-22実施）

事前登録した検出層（OR合成）で`scripts/backtest_vol_continuation_hybrid_4pairs_trainonly.py`を実行し、`scripts/evaluate_vol_continuation_hybrid_4pairs_trainonly_kpi.py`で正式判定した。

| 指標 | SYS-FX011ベースライン（N_BREAKOUT単独） | ハイブリッド（OR合成） |
|---|---|---|
| イベント数（dedup後） | 159 | 451 |
| トレード数（実効n） | 524 | 2159 |
| 月次シャープ | 2.940 | 4.507 |
| PF | 1.651 | 1.358 |
| ペイオフレシオ | 1.087 | 1.005 |
| 最大DD（ピーク比、月間=年間） | 11.28% | 23.01% |
| スプレッドコスト倍率 | 2.28 | 1.72 |
| permutation p値（day-block） | 0.035 | 0.015 |
| **KPI達成数（必須9項目）** | **6/9** | **5/9** |

### 解釈（正直な記録）

**必須ゲートの達成数はむしろ悪化した（6/9→5/9）。** 実効サンプル数・permutation有意性・月次シャープは大幅に改善したが、これらはベースラインの時点で既に「必須」達成済みだった項目であり、実質的な効果はなかった。一方、**最大DD（月間）が新たに「年間」も含めて未達に転じ**（月間のみ未達→月間・年間とも未達）、既に未達だったペイオフレシオ（1.087→1.005、建値付近まで悪化）・スプレッドコスト倍率（2.28→1.72、悪化）は改善しなかった。

原因分析: `00-prescreen.md`の診断2（Donchian完全置換）で観測された「n増加⇔質低下」のトレードオフが、OR合成でもほぼ同じ形で再現された。CALM_RATIO（2.0）による質フィルターは診断2の全置換（フィルターなし）よりは緩和的に効いている可能性があるが（イベント数はDonchian単独792件に対し451件に抑制）、それでも追加された約1600件のDonchian由来トレードが、既存524件の質を大きく薄めるには十分な規模だった。改善ループ第4試行（SYS-FX011、N/ZigZag/カレンダー窓の緩和でn増加を狙うも質が一貫して悪化）と同じパターンが、検出層を変えても再現した形になる。

**改善ループ第1試行（`PJ000004`「Q10」上限5回のうち1回目）として記録。** 司令塔判断待ち: (a) CALM_RATIOをより厳しい値に引き上げる再試行、(b) OR合成をやめてAND合成（Donchian継続かつN_BREAKOUT級の高ボラも同時に満たす、イベント数はごく少なくなる見込み）を試す、(c) 本設計の方向性自体を再検討する、のいずれで進めるか。

詳細: `research/method-notes/vol_continuation_hybrid_4pairs_trainonly_backtest.json`、`research/method-notes/vol_continuation_hybrid_4pairs_trainonly_kpi_evaluation.json`

## 改善ループ第2試行: 検出候補①②③ × H1トレンド判定不能除外フィルターの6パターン比較（2026-08-22実施）

### 背景
司令塔からの一連の依頼を受け、検出層の候補を①N_BREAKOUT単独（SYS-FX011ベースライン）・②N_BREAKOUT OR Donchian（CALM_RATIOなし）・③N_BREAKOUT OR (Donchian AND CALM_RATIO)（本specの事前登録設計）の3通りに整理し、さらに各候補に「H1ダウ理論トレンド方向が判定不能（zigzag threshold_atr=2.0、SYS-FX009 H1版の既存値を流用、先読みなし）だったトレンドイベントは検出時点でスキップする」フィルターを重ねた計6パターンをTrain単独で比較した。

### 結果

| 候補 | フィルター | トレード数 | 必須KPI | Sharpe | PF | ペイオフ | 最大DD | perm_p |
|---|---|---|---|---|---|---|---|---|
| ① | なし（ベースライン） | 524 | 6/9 | 2.940 | 1.651 | 1.087 | 11.28% | 0.035 |
| ① | 判定不能除外 | 300 | **7/9** | 2.397 | 1.874 | 1.148 | 8.69% | 0.031 |
| ② | なし | 4,211 | 4/9 | 2.874 | 1.208 | 0.945 | 45.33% | 0.065(非有意) |
| ② | 判定不能除外 | 2,594 | 5/9 | 3.032 | 1.286 | 0.988 | 33.26% | 0.033 |
| ③ | なし | 2,159 | 5/9 | 4.507 | 1.358 | 1.005 | 23.01% | 0.015 |
| ③ | 判定不能除外 | 1,395 | 6/9 | 3.120 | 1.400 | 1.035 | 19.30% | 0.025 |

### 解釈（正直な記録）

**6パターン中最良は「①(N_BREAKOUT単独) + 判定不能除外フィルター」で必須KPI 7/9。** これは元のベースライン①単独（6/9）を上回り、本EXP・SYS-FX011を通じて最良の結果である。未達の2項目はペイオフレシオ（1.148、閾値1.5に届かず）とスプレッドコスト倍率のみで、月次シャープ・PF・DD（月間・年間とも）・最大連続損失・実効n・permutation有意性はすべて達成した。

**当初の仮説（検出母集団をDonchianで広げてn不足を解消する）とは逆の結論が示唆される**: n増加方向（②③）はいずれもフィルター前後で改善しても①には届かず、むしろ**既存のN_BREAKOUT母集団自体を「H1トレンドが判定可能な状態」に絞り込む**方向が最も効いている。②③にも判定不能除外フィルターは有効に働いている（②4/9→5/9、③5/9→6/9）ため、判定不能除外フィルター自体は3候補いずれにも質を改善する効果を持つが、母集団の広さ（①<③<②の順にn増加）と質のトレードオフは依然として残る。

**重要な留保**: ①+フィルターの実効n=300は`min_n_trades_effective`閾値（≥300）のちょうど境界であり、統計的に脆弱。524→300という約43%の減少率がValidationでも同程度に生じた場合、Validationの実効nが300を下回りゲート未達に転じるリスクがある。この検証はまだTrain単独であり、Validationでの再現性は未確認。

改善ループ第2試行（`PJ000004`「Q10」上限5回のうち2回消化）として記録。司令塔判断待ち: (a) ①+判定不能除外フィルターをより厳密に検証する（Validation確認、有意性検定の見直し等）、(b) ②③の方向性を放棄し①ベースへ回帰する、(c) さらに別の質フィルターを模索する。

詳細: `research/method-notes/vol_continuation_ordonchian_nocalm_4pairs_trainonly_kpi_evaluation.json`、`research/method-notes/vol_continuation_candidates_trendfilter_4pairs_trainonly_backtest.json`

## 変更履歴
- 2026-08-22: 初版作成。検出層をN_BREAKOUT単独からOR合成（N_BREAKOUT既存条件 OR Donchian(20)継続+CALM_RATIO質フィルター）に変更する設計を事前登録。新規自由パラメータは導入しない
- 2026-08-22: Trainベースライン評価（改善ループ第1試行）を実施。必須KPI達成数が6/9→5/9へ悪化という負の結果。司令塔判断待ち
- 2026-08-22: 改善ループ第2試行として、検出候補①②③×H1トレンド判定不能除外フィルターの6パターンを比較。**「①(N_BREAKOUT単独)+判定不能除外フィルター」が必須KPI7/9で最良**（元のベースラインを上回る）。当初の「n増加でnr不足を解消する」という仮説とは逆に、既存母集団の絞り込みが有効という結果。実効n=300はmin_n_trades閾値の境界で脆弱、Validation未確認。司令塔判断待ち
